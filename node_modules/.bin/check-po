#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../i18n-abide/bin/check-po.sh" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../i18n-abide/bin/check-po.sh" "$@"
  ret=$?
fi
exit $ret
