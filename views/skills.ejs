<!DOCTYPE html>
<html lang="en" ng-app="ContactApp">
    <head>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.7.8/angular.min.js"></script>
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet"/>
        <meta charset="UTF-8">
        <title>Connect Skills</title>
    </head>
    <body>
        <div style="margin-top: 100px ;" class="container">
            <h5 style="margin-bottom: 50px ; text-align: center;">Skills for Connect</h5>
            <% if(success){%>  
                <div class="alert alert-danger alert-dismissible">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    <strong><%=success%></strong> 
                  </div>
          <%}else if(error){%>  
                <div class="alert alert-danger alert-dismissible">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    <strong><%=error%></strong> 
                  </div>                
         <% }%> 

            <form action="/skills" method="POST">
                
                <div class="form-group">
                    <input name="name" id="name"  class="form-control mb-20" placeholder="Skill Name" required/>
                </div>
<div class="form-group">
    <button type="submit"  class="btn btn-primary btn-block mt-20">Submit</button> 

</div>
            </form>
        </div>

    </body>
</html>